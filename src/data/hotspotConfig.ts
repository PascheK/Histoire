// Configurable timing window for hotspot visibility around a checkpoint.
// Adjust these values to tune how early/late a hotspot is visible.

export const HOTSPOT_EARLY_WINDOW_SEC = 2 // visible this many seconds BEFORE checkpoint
export const HOTSPOT_LATE_WINDOW_SEC = 0.20  // visible this many seconds AFTER checkpoint
